{
  "name": "inventory-management",
  "version": "1.0.0",
  "description": "Full-stack inventory management system with NestJS backend and Next.js frontend",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "create:module": "cd cli && npm run build && node dist/index.js",
    "backend:dev": "cd backend && npm run start:dev",
    "frontend:dev": "cd frontend && npm run dev",
    "build": "cd backend && npm run build && cd ../frontend && npm run build",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ../cli && npm install"
  },
  "keywords": ["inventory", "management", "nestjs", "nextjs", "typescript"],
  "author": "",
  "license": "MIT",
  "devDependencies": {},
  "dependencies": {}
}